services:
  - type: web
    name: football-season-predictor-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
      - key: AUTO_UPDATE_DATA
        value: "true"
      - key: DATA_UPDATE_INTERVAL_HOURS
        value: "6"
      - key: CORS_ORIGINS
        value: "*"
